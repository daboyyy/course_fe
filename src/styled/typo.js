import Typography from "@mui/material/Typography";
import { styled } from "@mui/system";

export const StyledErrorTypo = styled(Typography)({
  color: "red",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
});
